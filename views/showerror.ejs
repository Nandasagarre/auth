since this project is deployed on aws ecs; 
And it doesnt have any domain name linked, so..;
..which is why google auth is not working for now.

to test social auth, one could clone it from git in localhost and try it;

thanks for reading through :)
